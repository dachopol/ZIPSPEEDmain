import http from"node:http";import fs from"node:fs/promises";import path from"node:path";import{fileURLToPath}from"node:url";const root=path.dirname(fileURLToPath(import.meta.url)),port=Number(process.env.DEFAULT_APP_PORT||process.env.PORT||3000),types={".html":"text/html; charset=utf-8",".mjs":"text/javascript; charset=utf-8",".css":"text/css; charset=utf-8",".json":"application/json; charset=utf-8"};const server=http.createServer(async(req,res)=>{res.setHeader("X-Content-Type-Options","nosniff");res.setHeader("Referrer-Policy","no-referrer");res.setHeader("Permissions-Policy","geolocation=(), camera=(), microphone=()");if(!["GET","HEAD"].includes(req.method||"GET")){res.writeHead(405);res.end("Method Not Allowed");return}const u=new URL(req.url||"/","http://localhost");if(u.pathname==="/health"){res.writeHead(200,{"Content-Type":"application/json"});res.end(JSON.stringify({status:"ok"}));return}const rel=decodeURIComponent(u.pathname==="/"?"/index.html":u.pathname),file=path.normalize(path.join(root,rel));if(!file.startsWith(root)){res.writeHead(403);res.end("Forbidden");return}try{const data=await fs.readFile(file);res.writeHead(200,{"Content-Type":types[path.extname(file)]||"application/octet-stream","Cache-Control":"no-store"});if(req.method!=="HEAD")res.end(data);else res.end()}catch{res.writeHead(404);res.end("Not Found")}});server.listen(port,"0.0.0.0",()=>console.log("Zipspeed v50 listening on :"+port));