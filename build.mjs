import fs from"node:fs/promises";import path from"node:path";const root=process.cwd(),dist=path.join(root,"dist");await fs.rm(dist,{recursive:true,force:true});await fs.mkdir(path.join(dist,"src"),{recursive:true});for(const f of["index.html","metadata.json","package.json","version.json"])await fs.copyFile(path.join(root,f),path.join(dist,f));for(const f of["styles.css","app.mjs","measurement.mjs"])await fs.copyFile(path.join(root,"src",f),path.join(dist,"src",f));console.log("Zipspeed v51 premium static build created in dist/");